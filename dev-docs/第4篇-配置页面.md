根据代码分析，设置按钮的页面描述分布在以下文件中：

### 1. 菜单定义文件
**文件位置**：`app/src/main/res/menu/main_menu.xml`
```xml
<item
    android:id="@+id/action_settings"
    android:title="项目设置"
    android:icon="@drawable/ic_settings"
    app:showAsAction="ifRoom" />
```

### 2. 设置对话框布局文件
**文件位置**：`app/src/main/res/layout/dialog_settings.xml`

这个文件定义了设置页面的UI布局，包含：
- **时间设置**：开始/结束时间必填开关
- **花费设置**：显示花费开关、花费必填开关
- **提醒设置**：定时提醒开关、提醒间隔输入框

### 3. 设置逻辑处理文件
**文件位置**：`app/src/main/java/person/notfresh/noteplus/MainActivity.java`

在 `MainActivity.java` 中的 `showSettingsDialog()` 方法（第1859行）处理设置页面的逻辑：
- 加载当前设置值
- 处理用户交互
- 保存设置到数据库

### 总结

设置按钮的页面不是独立的Activity，而是：
1. **布局文件**：`dialog_settings.xml` - 定义UI界面
2. **逻辑处理**：`MainActivity.java` 中的 `showSettingsDialog()` 方法 - 处理交互逻辑
3. **菜单定义**：`main_menu.xml` - 定义菜单按钮

这是一个对话框形式的设置页面，直接在MainActivity中弹出显示。