# 需求设计

## 功能需求
- 费用统计

## 灵活配置
请帮我做一个增强，帮我增加一个表， setttings, 然后使用key, value的形式存储一些项目的配置到这个表里

然后给右上角增加一个菜单，可以显示对应的配置和修改对应的配置

现在我需要对每个项目增加一个配置，就是可以设置开始时间/结束时间是否设置为必填，如果是必填的，在保存的时候应该检查开始时间和结束时间是否符合设计要求

## 增加一个配置
请帮我做一个增强，帮我增加一个字段，花费，每一笔记录可以设置花费，默认是0，如果花费大于0，则显示在记录的旁边。

我们之前已经加过了配置功能，现在使用配置对花费进行管理，请帮我做增强

帮我增加第一个配置，就是可以设置花费是否设置为显示，如果是显示的，在界面显示它，否则隐藏它。
帮我增加第二个配置，就是可以设置花费是否设置为必填，如果是必填的，在保存的时候应该检查花费是否符合设计要求。

尽量少的改动原有代码。

